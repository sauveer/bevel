apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: provider-bevel-ac-auth-job
  annotations:
    fluxcd.io/automated: "false"
  namespace: provider-ns
spec:
  releaseName: provider-bevel-ac-auth-job
  chart:
    path: platforms/hyperledger-indy/charts/indy-auth-job
    git: https://github.com/sauveer/bevel.git
    ref: main
  values:
    metadata:
      name: provider-bevel-ac-auth-job
      namespace: provider-ns
    network:
      name: bevel
      kubernetes_url: https://7BFF136917E31CB861460C83A664A428.gr7.us-east-1.eks.amazonaws.com
    image:
      name: provider
      repository: alpine:3.9.4
    vault:
      address: http://a8e3897e42af646b49faf25942669568-584313495.us-east-1.elb.amazonaws.com:8200
      identity: bevel-ac
      admin_auth_path: kubernetes-provider-admin-auth
      policy: provider-bevel-ac-ro
      policy_content: path \"/provider/data/+/+/+/public*\" { capabilities = [ \"read\", \"list\" ] }
      auth_path: kubernetes-provider-bevel-ac-auth
    account:
      admin_service: provider-admin-vault-auth
      admin_role: rw
      service: provider-bevel-ac-vault-auth
      role: ro
